<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 使用MyBatis的API方式进行实现 -->
<mapper namespace="com.imudges.dao.UserDao">
    <select id="getUserById" resultType="User">
        SELECT *FROM waterpipe.user WHERE  u_id=#{uId};
    </select>
</mapper>
